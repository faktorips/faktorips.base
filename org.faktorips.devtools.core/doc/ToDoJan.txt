Jan's ToDos
===========
3.) Russendemo lauffähig machen
4.) Version-Info in die XML-Files aufnehmen. Core-Plugin und das jeweilige Plugin der Extension-Properties.
5.) Add-IPS-Nature: Libraries einrichten.

x.) DatatypeDefinition auch für Enums umstellen. Klären: Wie können bestehende Klassen, die einen Aufzählungstypen representieren
integriert werden.

Formelsprache:
Fehler im Compiler: GANZZAHL(WENN(vp.geschlecht=1; vp.alter; vp.alter / 10))

Was ist noch fuer 0.8 zu tun:
=============================
Wizard für IpsProject oder Umwandlung von bestehendem Java-Projekt.
Explizite Namensgebung für Produktseite.
Codegenerierung für Anpassungsstufen
(Produktkomponenteneditor-für Anpassungsstufen)
Validierung des IpsProjects und ausschalten des Builds, falls dort fehler enthalten sind.
Cleanup-Code: Entfernen der Referenzen auf Java-Types aus den Modellklassen.
Codegenerierung fuer Tabellen
Definition einer Nature fuer Erweiterungen?
DefaultValue fuer Attribute: flag, ob es einen default value gibt oder nicht.

Peter besprechen
 - Entfernen der PolicyClass aus dem TOC? Ersetzen durch ProductCmptClass!
 - Was ist noch am Builder zu tun?
	ANNOTATIONS	
	TOC
 - Referenz auf DateDatytpe im generierten TableAccess code rauswerfen.
- Pruefen, ob alle Zeichen wie z. B. öäü<> etc. in den XMLs gespeichert werden. CDATA Section verwenden?
 
Codegenerierung fuer Generationen
=================================
Einfuehren ProductCmptGeneration in Runtime, anpassen des XML-Einlesens
Aenderung der Codegenerierung, so dass auch Generations erzeugt werden und die bestehenden Methoden
auf Generations "umgeleitet werden".
Einfuehrung von: Namen fuer Produktbausteinklassen und Beziehungen!
TOC im Buildermechanismus richtig einbauen.
CompositeRuntimeRepository
Umstellen auf SAX Parser
ProductCmptInfo (lightweiht objekt mit informationen aus TOC-Entry)
Umbenennung des toc files in ...repository...
Sortierung der Generation, so dass die juengste als erstes gelesen wird.
Referenzen auf Money und Decimal aus den RuntimeTests entfernen

Wie gehen wir mit NLS um?
Zugriff auf die Preferences.